# 新 Codex 接手入口

你正在接手“阿栗私人小院”。这个项目是主人通过持续对话、截图反馈、实际使用和验收，与 Codex 一起迭代构建的。旧账号的对话记忆不应被视为依赖；仓库中的代码、测试和文档才是可迁移上下文。

## 先做什么

按顺序阅读：

1. `CODEX_START.md`：本页，建立接手边界。
2. `AGENTS.md`：工程铁律、目录、隐私与验收要求。
3. `docs/README.md`：文档总入口和当前能力结论。
4. `docs/01-PRD.md`：理解产品目标，不要把小院改成通用聊天壳。
5. `docs/03-系统架构.md`：理解本地、私人云和演示版。
6. `docs/05-数据同步与安全.md`：理解数据权威源、合并和隐私边界。
7. `docs/07-测试与运维手册.md`：修改后怎样完整验证。
8. `docs/08-Codex协作与迁移指南.md`：怎样继续与主人协作。

然后执行只读检查：

```bash
pwd
git status --short
git branch --show-current
git remote -v
python3 scripts/privacy_scan.py
python3 scripts/repository_readiness_test.py
```

不要因为看不到旧对话就重新设计项目，也不要用公开种子、旧电脑 JSON 或测试数据覆盖私人云端。

## 必须保留的产品决策

- 桌面版保持场景化小院；移动端单独重排，不牺牲桌面布局。
- 前端保持原生 HTML/CSS/JS，不引入 npm 构建体系。
- 私人云结构化数据以 Cloudflare KV 为权威源，本地是离线缓存。
- 删除使用 tombstone，旧设备不能复活；明确重新创建才允许 `revive`。
- 私人版与演示版使用不同 KV，演示版不能接触主人数据。
- 当前没有 R2，不能声称上传图片已永久跨设备同步。
- AI、工具或系统修改失败时必须明确说失败，不能把“已请求”说成“已完成”。
- GitHub 不保存口令、API Key、私人记忆、旅行、树洞、上传媒体或云端快照。
- 未收到主人明确指令，不提交、不推送、不部署。

## 每次任务的工作方式

1. 复述目标和边界，优先理解主人指出的实际使用问题。
2. 读取相关页面、状态层和测试，不凭截图猜代码。
3. 把自然语言反馈转成可验证的验收条件。
4. 做一组完整且相互一致的修改，不只改表面文案。
5. 补测试，运行相关回归；视觉变更同时检查桌面和 `393 x 852`。
6. 说明改了什么、验证了什么、还存在哪些真实限制。

当前请求优先级最高；若旧文档、旧实现或历史说法与主人最新要求冲突，应先核实当前代码和生产状态，再更新文档。

## 新任务开场模板

主人可以把下面这段直接发给新 Codex：

```text
请接手当前 cozy-estate 仓库。先阅读 CODEX_START.md、AGENTS.md 和 docs/README.md，
再按文档顺序理解 PRD、架构、同步安全与测试手册。先检查 git 状态和当前分支，
不要修改或覆盖任何私人数据，不要提交、推送或部署，除非我明确要求。

这个项目通过我与 Codex 的持续对话构建。请把我的使用反馈转成明确验收条件，
先读现有实现，再做小而完整的修改；桌面和手机分别验证。工具或系统操作失败时
直接说明无法完成，不要假装成功。完成后告诉我改动、测试结果和剩余限制。

本次任务是：<在这里写具体要求>
```

## 当前部署边界

- 私人版：`https://dcxin.neuralnode.top`
- 演示版：`https://demo.neuralnode.top`
- 主分支：`dev`
- 本地启动：`python3 scripts/cozy_server.py --port 8766`

Cloudflare Secret、KV 数据和域名权限不在 Git 中。换电脑或账号后，需要主人重新提供对应平台权限；不要要求主人把密钥粘贴进聊天或代码。
