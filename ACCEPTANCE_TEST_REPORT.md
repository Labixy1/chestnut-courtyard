# 栗壳小院发布验收报告

更新时间：2026-08-09

## 1. 首页与全局运行

- 页面可在本地服务和 `file://` 两种模式打开，核心入口完整。
- 阿栗在线状态、当地时间、天气和两小时缓存状态可显示。
- 八个房间入口可进入，返回小院不出现错误中间画面。
- 桌面与窄屏无文字遮挡、横向溢出和不可点击控件。
- PWA manifest、Service Worker 和离线静态资源路径有效。

## 2. 公告板

- 最新巡报、往期巡报、分类归档和稍后看均可打开。
- 往期默认收起，展开后显示完整热点、正文、案例总结和阿栗建议。
- 留言任务显示排队、运行、完成或失败，离开再回来状态仍存在。
- 同 URL、同标题和留言内部重复资讯不会重复展示。
- 原文、复制链接、提取工具、稍后看和栗夹操作可用。
- 更新失败会显示错误，不会长期伪装成运行中。
- 周一、周三、周六本地计划与云端每两天巡报计划符合配置。

## 3. 黑板

- 每日题、换一题、历史归档、聚类分类和再做一次可用。
- 同一道题只保留一个归档入口，多次作答保留历史尝试。
- 提交后按钮锁定，AI 批改、逐点评分、阿栗帮答、标准答案和下一题正常。
- 题边阿栗回答与当前题相关，并将补充资料归入当前题。
- 出题方向只作为多方向中的一个参考，不会永久垄断题目。
- 评分失败或模型失败有明确错误/本地兜底。

## 4. 工具箱与 Skills

- 全部分类包含普通工具和外部 Skills，卡片尺寸及按钮一致。
- 链接解析可生成类别、用途、关键能力、地址、场景和例子。
- 新增、编辑、移动、删除、稍后使用和价格空状态可用。
- 系统内置 Skills 只在密阁系统骨架显示，中英文标题和中文说明完整。
- 15 个固定 Skill、动态 Skill 注册和工具契约通过验证。

## 5. 成长田

- 五块土地、空地、七阶段植物、单块浇水、一键浇水和一键收成可用。
- 成熟后收进背包，土地恢复为空并可继续种植。
- 新对话不会删除旧对话，历史可切换追溯。
- 问答自动归入可演进的知识专题，专题默认收起。
- 专题本和背包仅在有未读更新时显示 `NEW`，打开后跨刷新保持已读。
- 生长门槛为 22 点，已有植物不会因新门槛倒退。
- 黑板、树洞和生活记录只以安全摘要提供养分，不泄漏树洞原文。

## 6. 树洞

- 签语与对话模式可切换，语音识别文本实时进入输入框。
- 草坪连续五次打开历史，再次关闭正常。
- 一天最多生成一张真正重要的影像，无有效内容时不生成。
- 生图成功显示图片，失败显示真实错误，不伪装生成中。
- 影像文字、签语、对话均可编辑和删除，删除文案统一。
- 树洞原文只进入封存密库，不进入普通档案或普通任务提示词。

## 7. 旅行与照片墙

- 旅行地点、日期、描述、照片和感悟可新增、编辑、删除。
- 进入旅行感悟时输入框为空，已保存摘要仍保留。
- 感悟保存后清空输入，并自动更新旅行摘要。
- 旅行记录可进入详情查看照片和完整记录。
- 照片墙按照片集管理，可新增、删除、移动和时间排序。
- 小院场景合集只包含公开场景背景，不含密阁和私人照片。
- 图片比例不一致时使用完整图而非强制裁切。

## 8. 卧室、密阁与权限

- 开关灯热点准确且只有一个可见控制。
- 密阁入口保持普通鼠标样式，连续三次可打开。
- 掌院权限开启后永久保存，普通权限不能修改系统骨架。
- 系统修改创建快照，执行精确补丁，验证失败自动回滚。
- 任务历史显示运行、成功、失败和撤销证据。

## 9. 记忆系统专项

- 非敏感交互写入不可变证据；单次信号只成为候选。
- 重复证据达到阈值后自动生效，明确要求记住立即生效。
- 同槽位新偏好覆盖旧偏好并保留纠正历史。
- 档案只引用生效卡片，卡片变化后自动重写文本档案。
- 当前任务只召回相关记忆，当前指令优先于历史偏好。
- 树洞、密阁和最高级秘密保持封存隔离。
- 分类支持自动形成、新建、改名、移动、合并和删除。
- 彻底遗忘会删除卡片与证据，并用 tombstone 防止旧副本复活。
- 重启不丢失、不重复；旧格式迁移幂等；短期候选可自动过期。
- AI 蒸馏支持语义合并、冲突识别、证据阈值和低信号保留。
- 模型输出引用未知卡片、错误分类或无效 JSON 时整批拒绝且不改数据。
- 蒸馏保存前后档案、差异、审计和回滚数据；重复无变化时跳过。
- 每日维护无需人工逐条确认，失败保留原有确定性档案。

## 10. 模型、媒体、语音与天气

- 本地主路由为 DeepSeek V4 Flash，失败切换 GPT-5.6 Luna。
- 云端使用同样主备顺序，均失败时可切 Workers AI。
- Seedream、GPT Image、Nano Banana 和 Seedance 协议契约有效。
- 当前产品只优先触发生图，不自动触发视频。
- 所有语音入口使用同一实时转写组件；拒绝权限时有明确提示。
- 天气每两小时刷新，断网使用最近缓存或晴天。

## 11. 隐私、分发与部署

- Git 追踪文件不包含 API Key、私人记忆、树洞、照片、日志或密阁数据。
- `preview` 使用空白种子、禁止写入且不绑定主人 KV。
- `owner` 使用口令和 HttpOnly 会话、KV 私人状态及非公开缓存策略。
- 主人域名未登录返回 401/403，公开预览无需登录但只读。
- 两套云构建所有资源引用存在，发布包体积满足快速演示要求。
- GitHub Actions 在 push/PR 自动执行隐私、语法、Worker、构建和资源测试。

## 验收结果

### 自动化与真实数据

- 15 组发布检查全部通过：HTML 语法、公告板、系统运行、服务 API、15 个固定 Skill、模型协议、云 Worker、云资源、仓库就绪、隐私扫描、Python 编译、记忆 E2E、记忆蒸馏、主人包和公开包隔离。
- 真实模型调用成功，当前主路由为 `DeepSeek V4 Flash`，失败后切换 `GPT-5.6 Luna`；云 Worker 另有 Workers AI 末级兜底。
- 真实天气 API 返回有效缓存，缓存未过期，场景可根据天气切换。
- 桌面 1280px 和移动端 390px 检查：首页、成长田、树洞、旅行、记忆档案均无横向溢出、断图或控制台错误；公告板、黑板、工具箱、照片墙弹层在 390px 下宽度正确。

### 记忆专项结果

- 修复前备份：`/tmp/cozy-memory-before-integrity-fix-20260809-194120`。
- 修复后真实数据为 85 条普通事件、42 条封存事件、65 张卡片、13 个分类和 9 个档案章节。
- 普通事件与封存事件 ID 均唯一、互不重叠；普通事件中不再含树洞封存原文；所有卡片证据与分类引用均可解析。
- 完整性迁移记录：移出 46 条封存记录、删除 6 条重复 ID、补齐 21 条历史证据；再次启动保持幂等。
- 蒸馏最近一次状态为完成，模型为 DeepSeek，失败字段为空；无效 JSON、未知引用和错误分类的整批拒绝测试通过。

### 发布结果

- 主人版已部署到 `https://dcxin.neuralnode.top`，口令、HttpOnly 会话、KV 私人状态和禁止公开缓存已通过 Worker 自动化测试。
- 演示版已部署到 `https://demo.neuralnode.top`，只含示例种子，所有写接口拒绝，且不绑定主人 KV、模型密钥、记忆、树洞、照片或日志。
- 两套发布包均约 8 MB，Cloudflare 资源上传和 Worker 触发器部署成功。
- 当前公司网络会在请求抵达 Cloudflare 前用“云壳”拦截新域名，因此本机无法完成公网终端验收；需使用手机流量或非公司网络各打开一次确认。

### 仍需人工验收

- 真实麦克风权限与中文连续口述：浏览器必须由使用者授权，自动化不能代替实际说话。
- 非公司网络打开主人版并完成一次正确口令登录；打开演示版并确认无需登录、刷新后仍为示例数据。
- 树洞和记忆页各有少量小于 28px 的次要图标按钮，当前可用，但后续可统一到更舒适的触控尺寸。
